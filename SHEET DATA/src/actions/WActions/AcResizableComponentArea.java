package actions.WActions;

import java.io.IOException;
import java.io.ObjectInputStream;

import funktionBundles.Var;
import obj.UIobj.right.ImageAreaHierarchy;
import obj.UIobj.right.TextAreaHierarchy;
import obj.Wobj.ResizableComponentArea;
import obj.Wobj.textarea.TextAreaPropperties;
import obj.Wobj.textarea.TextArea;

public class AcResizableComponentArea 
{
	public static void run(int seitenzahl , int sizeX, int sizeY, int boundsX, int boundsY)
	{
		/*
		
		textareaTest text = new textareaTest(seitenzahl,Var.componentareaCount, sizeX, sizeY, boundsX, boundsY);
		Var.resizableComponentArea[Var.componentareaCount] = text;
		
		Var.emptyPage[seitenzahl].setLayer(Var.resizableComponentArea[Var.componentareaCount], 2);
		Var.emptyPage[seitenzahl].add(Var.resizableComponentArea[Var.componentareaCount]);
		Var.emptyPage[seitenzahl].revalidate();
		
		Var.componentareaCount = Var.componentareaCount + 1;	
		*/
	}
}
